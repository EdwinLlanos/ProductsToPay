<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ProductTextViewName">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/colorFontNormal</item>
        <item name="android:textSize">@dimen/fontSizeName</item>
        <item name="android:layout_centerInParent">true</item>
        <item name="android:text">@string/testDefault</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_margin">16dp</item>
    </style>

    <dimen name="fontSizeName">18sp</dimen>
    <dimen name="marginSizeName">12sp</dimen>
    <string name="testDefault">Default</string>
    <color name="colorFontNormal">#FFFFFF</color>
</resources>
